# /etc/ufw/ufw.conf
#
# Set to yes to apply rules to support IPv6 (no means only IPv6 on loopback
# accepted). You will need to 'disable' and then 'enable' the firewall for
# the changes to take affect.
IPV6=yes

# Set the default input policy to DROP, default output policy to ACCEPT, and
# default forward policy to ACCEPT. Uncomment the appropriate line to configure
# the policy.
DEFAULT_INPUT_POLICY="DROP"
DEFAULT_OUTPUT_POLICY="ACCEPT"
DEFAULT_FORWARD_POLICY="ACCEPT"

# Set the default application policy to the listed profile. See
# /usr/share/ufw/applications.available/ for all available options.
# The recommended default is "ufw-deny-all".
DEFAULT_APPLICATION_POLICY="ufw-deny-all"
